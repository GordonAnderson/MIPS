diff --git a/include/DCbiasCtrl.h b/include/DCbiasCtrl.h
index e2ca25f..cf10e63 100644
--- a/include/DCbiasCtrl.h
+++ b/include/DCbiasCtrl.h
@@ -3,7 +3,7 @@
 
 #if DCBanalog
 
-#include "DCBias.h"
+#include "DCbias.h"
 
 #define TCA9534add    0x21    // DIO TWI base address
 
diff --git a/include/DMSDMSMB.h b/include/DMSDMSMB.h
index 4280bdf..9a7f4c7 100644
--- a/include/DMSDMSMB.h
+++ b/include/DMSDMSMB.h
@@ -1,6 +1,6 @@
 #ifndef DMSDMSMB_h
 #define DMSDMSMB_h
-#include "hardware.h"
+#include "Hardware.h"
 
 #if DMSDMSMB
 #define SIGNATURE  0xAA55A5A5
diff --git a/include/RFamp.h b/include/RFamp.h
index 5333452..0a4f2ae 100644
--- a/include/RFamp.h
+++ b/include/RFamp.h
@@ -48,6 +48,12 @@
                                       // Pin 11 on EXT1 connectes to CPLD pin 30,
                                       // called CPLD_CLEA but not used
 
+// 18 bit DAC (AD5680) full scale count. The AD5680 24 bit frame is:
+//   DB23:DB22 = don't care, DB21:DB20 = PD1:PD0, DB19:DB2 = data, DB1:DB0 = don't care
+// A count outside 0..RFAdac18BITMAX shifts into the power down bits and disables the
+// DAC output, so counts must be clamped, not allowed to wrap.
+#define RFAdac18BITMAX     262143
+
 // ADC input channels
 #define RFAadcDCV             0
 #define RFAadcDCI             1
@@ -58,6 +64,11 @@
 #define RFAadcRFLA            6
 #define RFAadcRFLB            7
 
+// The low range ADC calibration array ADCchansLR[] holds only the two RF level
+// channels. Index it as ADCchansLR[chan - RFAadcLR_BASE] so the mapping survives
+// any future renumbering of the RFAadc* channel defines.
+#define RFAadcLR_BASE      RFAadcRFLA
+
 // DAC output channels
 #define RFAdacDRIVE           0
 #define RFAdacSETPOINT        1
diff --git a/include/SC16IS740.h b/include/SC16IS740.h
index e38c012..5d3cafd 100644
--- a/include/SC16IS740.h
+++ b/include/SC16IS740.h
@@ -4,7 +4,7 @@
 #if FAIMSFBcode
 
 #include "Arduino.h"
-#include <WIRE.h>
+#include <Wire.h>
 
 #define  RHR				0x00
 #define  THR				0x00
diff --git a/include/Variants.h b/include/Variants.h
index b5aff36..7d785b1 100644
--- a/include/Variants.h
+++ b/include/Variants.h
@@ -26,7 +26,7 @@
 
 #define DCBtripsESI
 
-#include "errors.h"
+#include "Errors.h"
 #include "DIO.h"
 #include "Hardware.h"
 #include "TWIext.h"
diff --git a/src/ADCdrv.cpp b/src/ADCdrv.cpp
index 6025ae3..40a55c6 100644
--- a/src/ADCdrv.cpp
+++ b/src/ADCdrv.cpp
@@ -470,12 +470,18 @@ void ADCrbRead(int start, int num)
 bool ADCacquireWait(void)
 {
   if((ADCclock->getStatus() & TC_SR_CLKSTA) == 0) return true;
-  // Calculate the number of 50uS time units in the acquire
-  int units = (ADCrate * 20000) / ADCnumsamples;
+  // Calculate the number of 50uS time units in the acquire, plus 1mS of margin.
+  //   acquire time in seconds        = ADCnumsamples / ADCrate
+  //   acquire time in 50uS units      = (ADCnumsamples * 20000) / ADCrate
+  // The previous expression had the ratio inverted, which produced a 40 second
+  // timeout at the default 5000 samples / 200kHz.
+  int units = ((int64_t)ADCnumsamples * 20000) / ADCrate + 20;
   for(int i=0;i<units;i++)
   {
     delayMicroseconds(50);
     if((ADCclock->getStatus() & TC_SR_CLKSTA) == 0) return true;
+    // Pet the watchdog; a long acquire can otherwise outlast the 4 second interval
+    if((i & 0x3FF) == 0x3FF) WDT_Restart(WDT);
   }
   // if here we timed out
   return false;
@@ -485,12 +491,14 @@ bool ADCacquireWait(void)
 // NAK is returned if the vector has not been alocated or the number of recorded
 // vectors is 0. 
 // If the ADCclock is running then an acquire is in progress and this routine will wait.
-// Returns -1 on timeout
+// Returns false on timeout or if no vector has been recorded
 bool ADCfindSum(int *sum)
 {
   if(ADCbuffer == NULL) return false;
   if(ADCvectorNum <= 0) return false;
-  if(!ADCacquireWait()) return(-1);
+  // A timeout must report failure. Returning -1 from a bool function yields true,
+  // so the caller would sum whatever stale data was left in the buffer.
+  if(!ADCacquireWait()) return false;
   *sum = 0;
   for(int i=0;i<ADCnumsamples;i++) *sum += ADCbuffer[i];
   return true;
diff --git a/src/Analog.cpp b/src/Analog.cpp
index e115aee..1522d2c 100644
--- a/src/Analog.cpp
+++ b/src/Analog.cpp
@@ -20,12 +20,12 @@
 #include <Thread.h>
 #include <ThreadController.h>
 #include <Adafruit_ADS1015.h>
-#include "wire.h"
+#include "Wire.h"
 #include "SD.h"
 #include "string.h"
 #include "Serial.h"
 #include "Analog.h"
-#include "dialog.h"
+#include "Dialog.h"
 #include "DCbias.h"
 #include "RFdriver.h"
 #include "Variants.h"
diff --git a/src/Filament.cpp b/src/Filament.cpp
index 464307f..131aaf9 100644
--- a/src/Filament.cpp
+++ b/src/Filament.cpp
@@ -85,12 +85,12 @@
 #include <Thread.h>
 #include <ThreadController.h>
 #include <stdarg.h>
-#include "wire.h"
+#include "Wire.h"
 #include "SD.h"
 #include "string.h"
 #include "Serial.h"
 #include "Filament.h"
-#include "dialog.h"
+#include "Dialog.h"
 #include "Variants.h"
 
 extern Menu MainMenu;
diff --git a/src/MIPS.cpp b/src/MIPS.cpp
index e868ad8..4a13e50 100644
--- a/src/MIPS.cpp
+++ b/src/MIPS.cpp
@@ -1,6 +1,6 @@
 #include <DueFlashStorage.h>
 #include "SD.h"
-#include "utility/Sd2card.h"
+#include "utility/Sd2Card.h"
 #include "SPI.h"
 #include "Adafruit_GFX.h"
 #include "Adafruit_ILI9340.h"
diff --git a/src/RFamp.cpp b/src/RFamp.cpp
index cb89daf..d9ab51d 100644
--- a/src/RFamp.cpp
+++ b/src/RFamp.cpp
@@ -77,7 +77,7 @@
 
 //
 #include <Arduino.h>
-#include "RFAMP.h"
+#include "RFamp.h"
 #include "Variants.h"
 #include "Hardware.h"
 #include "Table.h"
@@ -296,7 +296,9 @@ const Commands  RFAMPCmdArray[] = {
   {"SRFALEV", CMDfunctionStr, 2, (char *)RFAsetLevel},       // Sets the RF output voltage setpoint
   {"GRFALEV", CMDfunction, 1, (char *)RFAgetLevel},          // Returns the RF output voltage setpoint
   {"GRFAVPPA", CMDfunction, 1, (char *)RFAgetVPPA},          // Returns the RF output A actual level
-  {"GFRAVPPB", CMDfunction, 1, (char *)RFAgetVPPB},          // Returns the RF output B actual level
+  {"GRFAVPPB", CMDfunction, 1, (char *)RFAgetVPPB},          // Returns the RF output B actual level
+  {"GFRAVPPB", CMDfunction, 1, (char *)RFAgetVPPB},          // Legacy misspelling of GRFAVPPB, retained for
+                                                            // compatibility. Use GRFAVPPB in new host code.
   {"GRFAPWR", CMDfunction, 1, (char *)RFAgetPWR},            // Returns the RF amp RF forward pwr
   {"SRFARNG", CMDfunctionStr, 2, (char *)RFAsetRange},       // Sets the QUAD RF maximum RF level
   {"GRFARNG", CMDfunction, 1, (char *)RFAgetRange},          // Returns the QUAD RF maximum RF level
@@ -357,18 +359,24 @@ void setRFADAC(int brd, int channel, float value, float gc)
 {
    if(RFAarray[brd]->Rev == 3)
    {
-      int ch = channel;
-      if(channel == RFAdacSETPOINT) ch = RFAcpldCS_RFSP;
-      if(channel == RFAdacDRIVE)    ch = RFAcpldCS_RFD;
+      // Only DRIVE and SETPOINT are routed to 18 bit DACs on rev 3. AUX1 and AUX2
+      // are not: passing them through would place their channel numbers (2 and 3)
+      // on the CPLD shift register, toggling RFAcpldCLOSED and RFAcpldRANGE.
+      int ch;
+      if(channel == RFAdacDRIVE)         ch = RFAcpldCS_RFD;
+      else if(channel == RFAdacSETPOINT) ch = RFAcpldCS_RFSP;
+      else
+      {
+         AD5625(RFAarray[brd]->DACadr,channel,Value2Counts(value,&RFAarray[brd]->DACchans[channel],gc),3);
+         return;
+      }
       // Here for 18 bit DAC mode
-      int cnts;
       if((channel == RFAdacSETPOINT) && ((RFACPLDimage[brd] & (1<<RFAcpldRANGE))) == 0)
       {
-         Set_18bitDAC(brd,ch,Value2Counts(value,&RFAarray[brd]->DACchansLR,gc,262143));
+         Set_18bitDAC(brd,ch,Value2Counts(value,&RFAarray[brd]->DACchansLR,gc,RFAdac18BITMAX));
          return;
       }
-      Set_18bitDAC(brd,ch,cnts=Value2Counts(value,&RFAarray[brd]->DACchans[channel],gc,262143));
-      serial->println(cnts);
+      Set_18bitDAC(brd,ch,Value2Counts(value,&RFAarray[brd]->DACchans[channel],gc,RFAdac18BITMAX));
       return;
    }
    if((channel == RFAdacSETPOINT) && ((RFACPLDimage[brd] & (1<<RFAcpldRANGE))) == 0)
@@ -464,22 +472,16 @@ void RFampCalibrate(void)
   // Low range calibration
   if((CaledFlag & 0xC0) == 0xC0)
   {
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].m = (float)(VpP2adcLR - VpP1adcLR) / (RFAD->RangeThreshold * 0.5);
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].b = (float)VpP1adcLR - (RFAD->RangeThreshold * 0.25) * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].m;
 
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].m = (float)(VpP2adcLR - VpP1adcLR) / (RFAD->RangeThreshold * 0.75 - 20.0);
-     //RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].b = (float)VpP1adcLR - 10.0 * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].m;
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].b = (float)VpP2adcLR - (RFAD->RangeThreshold * 0.75) * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-6].m;
+     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-RFAadcLR_BASE].m = (float)(VpP2adcLR - VpP1adcLR) / (RFAD->RangeThreshold * 0.75 - 20.0);
+     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-RFAadcLR_BASE].b = (float)VpP2adcLR - (RFAD->RangeThreshold * 0.75) * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLA-RFAadcLR_BASE].m;
      CaledFlag &= ~0xC0;
   }
   if((CaledFlag & 0x300) == 0x300)
   {
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].m = (float)(VnP2adcLR - VnP1adcLR) / (RFAD->RangeThreshold * 0.5);
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].b = (float)VnP1adcLR - (RFAD->RangeThreshold * 0.25) * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].m;
 
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].m = (float)(VnP2adcLR - VnP1adcLR) / (RFAD->RangeThreshold * 0.75 - 20.0);
-     //RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].b = (float)VnP1adcLR - 10.0 * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].m;
-     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].b = (float)VnP2adcLR - (RFAD->RangeThreshold * 0.75) * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-6].m;
+     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-RFAadcLR_BASE].m = (float)(VnP2adcLR - VnP1adcLR) / (RFAD->RangeThreshold * 0.75 - 20.0);
+     RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-RFAadcLR_BASE].b = (float)VnP2adcLR - (RFAD->RangeThreshold * 0.75) * RFAarray[SelectedRFAboard]->ADCchansLR[RFAadcRFLB-RFAadcLR_BASE].m;
      CaledFlag &= ~0x300;
   }
   if((CaledFlag & 0xC00) == 0xC00)
@@ -518,23 +520,43 @@ void SetRangeLow(int brd, bool Send)
 //
 // - RFD = 1, 9 is the internal shift register bit
 // - RFSP = 2, 10 is the internal shift register bit
-// This DAC is 18 bit, 0 to 5V output
+// This DAC is an AD5680, 18 bit, 0 to 5V output.
+//
+// CS_x asserts on (BRDSEL AND address match AND latched enable bit) per the CPLD
+// equations. It is NOT gated by the SPI CS line, so the enable bit is armed only for
+// the duration of this transaction and cleared on the way out. RFACPLDimage[] must
+// never carry these bits persistently.
+//
+// SetAddress(0) is what raises SYNC and is therefore the AD5680 output update edge;
+// the part has no LDAC pin. It must happen before the enable bit is cleared.
 void Set_18bitDAC(int brd, int DACchannel, int value)
 {
-  static bool init=true;
-  // Lower CS
+  AtomicBlock< Atomic_RestoreState > a_Block;
+
+  // An out of range count shifts into the AD5680 PD1:PD0 bits, which powers the
+  // output down (value > max) or three-states it (value < 0), rather than simply
+  // producing a wrong voltage. Clamp hard.
+  if(value < 0)                value = 0;
+  if(value > RFAdac18BITMAX)   value = RFAdac18BITMAX;
+
+  // Arm the chip select enable for this DAC
   SendFRAcpldCommand(RFAarray[brd]->CPLDspi, RFACPLDimage[brd] | (1 << DACchannel));
+  // Assert CS by placing the decoded address on the bus
   if(DACchannel == RFAcpldCS_RFD)       SetAddress(RFAcpldADD_RFD);
   else if(DACchannel == RFAcpldCS_RFSP) SetAddress(RFAcpldADD_RFSP);
   else if(DACchannel == RFAcpldCS_P1)   SetAddress(RFAcpldADD_P1);
   else if(DACchannel == RFAcpldCS_P2)   SetAddress(RFAcpldADD_P2);
-  // Send Data
-  if(init)
+  else
   {
-    init = false;
-    SPI.setDataMode(SPI_MODE2);
-    SPI.setClockDivider(12);
+    // Not a valid 18 bit DAC select, undo the enable and exit without clocking data
+    SendFRAcpldCommand(RFAarray[brd]->CPLDspi, RFACPLDimage[brd] & ~(1 << DACchannel));
+    return;
   }
+  // Send Data. AD5680 clocks data on the falling edge, SPI mode 2. Set the mode and
+  // divider on every call; SendFRAcpldCommand, SetDDSfrequency and AD5668 all
+  // reconfigure SPI in between.
+  SPI.setDataMode(SPI_MODE2);
+  SPI.setClockDivider(12);
   value <<= 2;
   uint8_t *b = (uint8_t *)&value;
   SPI.transfer(b[2]);
@@ -657,6 +679,10 @@ void RestoreRFAsettings(bool NoDisplay)
      {
        // Here if the name matches so copy the data to the operating data structure
        rfa_data.EEPROMadr = RFAarray[b]->EEPROMadr;
+       // Size is read from EEPROM and cannot be trusted. An image written by a newer
+       // firmware with a larger RFAdata would overflow the allocation.
+       if(rfa_data.Size > (int16_t)sizeof(RFAdata)) rfa_data.Size = sizeof(RFAdata);
+       if(rfa_data.Size < 0) rfa_data.Size = 0;
        memcpy(RFAarray[b], &rfa_data, rfa_data.Size);
        RFAarray[b]->Size = sizeof(RFAdata);
      } 
@@ -780,11 +806,12 @@ void RFA_init(int8_t Board, int8_t addr)
   // If Rev equals 3 setup the requirements
   if(RFAarray[Board]->Rev == 3)
   {
-    RFACPLDimage[Board] |= (1 << RFAcpldCS_RFD) | (1 << RFAcpldCS_RFSP) | (1 << RFAcpldCS_P1) | (1 << RFAcpldCS_P2);
-    // Setup the SPI interface
-
-    // Set the 18bit DACs to initial values
-    
+    // NOTE: the four 18 bit DAC chip select enable bits (RFAcpldCS_*) are deliberately
+    // NOT set here. Per the CPLD equations, CS_x asserts on
+    //   (BRDSEL AND address match AND latched enable bit)
+    // with no gating from the SPI CS line. Leaving an enable latched would let that
+    // DAC's select follow the raw address bus. The enables are armed and cleared
+    // inside Set_18bitDAC for the duration of a single transaction only.
   }
   // Set full scale limit
   RFAdialogEntrySetPoint.Max = RFAarray[Board]->FullScale;
@@ -813,19 +840,49 @@ void RFA_init(int8_t Board, int8_t addr)
   }
   NumberOfRFAchannels++;
   if(NumberOfRFAchannels > 1) de->Max = 2;
+  // The CPLD has no reset input (CPLD_CLEA is unconnected), so drive it to a known
+  // state explicitly rather than relying on the change detection in RFA_loop.
+  SelectBoard(Board);
+  SendFRAcpldCommand(RFAarray[Board]->CPLDspi, RFACPLDimage[Board]);
+  RFAstates[Board]->CPLDimage = RFACPLDimage[Board];
   SelectBoard(SelectedRFAboard);
 }
 
+// Write both quad pole DACs from the current PoleBias and ResolvingDC settings.
+// Rev 3 only. Both poles are always written together because each pole voltage is a
+// function of both parameters, so there is no meaningful "set just one" operation.
+//
+// Reads both parameters from the module structure. Every caller of SetPoleBias and
+// SetResolvingDC assigns the structure before calling, and the one call site that
+// passes explicit zeros is reached only when the module is disabled, where this
+// function zeros both anyway.
+// Calculate the RF level detector gain compensation factor from the calibration
+// frequency and the current operating frequency. Returns 1.0 when compensation is
+// disabled. Extracted from three identical copies in RFA_loop, RFAupdateQUAD and
+// RFAacquire.
+static float RFAgainComp(int brd)
+{
+  if(!RFAarray[brd]->EnableComp) return 1.0;
+  return 1.0 + (RFAarray[brd]->CalFrequency - RFAarray[brd]->Freq)/1000000.0 * RFAarray[brd]->FreqComp;
+}
+
+static void RFAupdatePoles(int brd)
+{
+   float bias = RFAarray[brd]->PoleBias;
+   float rdc  = RFAarray[brd]->ResolvingDC;
+
+   if(!ResolvingDCenable)      rdc = 0;
+   if(!RFAarray[brd]->Enabled) bias = rdc = 0;
+   SelectBoard(brd);
+   Set_18bitDAC(brd, RFAcpldCS_P1, Value2Counts( rdc + bias,&RFAarray[brd]->DACresDCCtrl[0],1.0,RFAdac18BITMAX));
+   Set_18bitDAC(brd, RFAcpldCS_P2, Value2Counts(-rdc + bias,&RFAarray[brd]->DACresDCCtrl[1],1.0,RFAdac18BITMAX));
+}
+
 void SetPoleBias(int brd, float value)
 {
    if(RFAarray[brd]->Rev == 3)
    {
-     float rdc = RFAarray[brd]->ResolvingDC;
-     if(!ResolvingDCenable) rdc = 0;
-     if(!RFAarray[brd]->Enabled) rdc = value = 0;
-     // Rev 3 used 18 bit DAC
-     Set_18bitDAC(brd, RFAcpldCS_P1, Value2Counts( rdc + value,&RFAarray[brd]->DACresDCCtrl[0],1.0,262143));
-     Set_18bitDAC(brd, RFAcpldCS_P2, Value2Counts(-rdc + value,&RFAarray[brd]->DACresDCCtrl[1],1.0,262143));
+     RFAupdatePoles(brd);
      return;
    }
    // Set the PoleBias value
@@ -852,12 +909,7 @@ void SetResolvingDC(int brd, float value, bool now = false)
 {
    if(RFAarray[brd]->Rev == 3)
    {
-     // Rev 3 used 18 bit DAC
-     float bias = RFAarray[brd]->PoleBias;
-     if(!ResolvingDCenable) value = 0;
-     if(!RFAarray[brd]->Enabled) bias = value = 0;
-     Set_18bitDAC(brd, RFAcpldCS_P1, Value2Counts( value + bias,&RFAarray[brd]->DACresDCCtrl[0],1.0,262143));
-     Set_18bitDAC(brd, RFAcpldCS_P2, Value2Counts(-value + bias,&RFAarray[brd]->DACresDCCtrl[1],1.0,262143));
+     RFAupdatePoles(brd);
      return;
    }
    // Set the ResolvingDC value
@@ -908,12 +960,8 @@ void RFA_loop(void)
     SelectBoard(brd);
     // Calculate the gain compensation factor based on the calibration frequency and the current operating
     // frequency.
-    if(RFAarray[brd]->EnableComp)
-    {
-       // Here if compensation is enabled so calculate the correction factor. 
-       gc = 1.0 + (RFAarray[brd]->CalFrequency - RFAarray[brd]->Freq)/1000000.0 * RFAarray[brd]->FreqComp;
-    }
-    else gc = 1.0;
+    gc = RFAgainComp(brd);
+
     if(QUADupdateFlag) QUADupdate(brd);
     // Look for any changed values and update
     if((RFAarray[brd]->Enabled != RFAstates[brd]->Enabled) || RFAupdate)
@@ -1052,12 +1100,12 @@ void RFA_loop(void)
       }
       else
       {
-         val = Counts2Value(ADCvals[RFAadcRFLA],&RFAarray[brd]->ADCchansLR[RFAadcRFLA-6],gc);
+         val = Counts2Value(ADCvals[RFAadcRFLA],&RFAarray[brd]->ADCchansLR[RFAadcRFLA-RFAadcLR_BASE],gc);
          if(RFAstates[brd]->RFVPpp == -1) RFAstates[brd]->RFVPpp = val;
          else RFAstates[brd]->RFVPpp = Filter * val + (1-Filter) * RFAstates[brd]->RFVPpp;
-         val = Counts2Value(ADCvals[RFAadcRFLB],&RFAarray[brd]->ADCchansLR[RFAadcRFLB-6],gc);
+         val = Counts2Value(ADCvals[RFAadcRFLB],&RFAarray[brd]->ADCchansLR[RFAadcRFLB-RFAadcLR_BASE],gc);
          if(RFAstates[brd]->RFVNpp == -1) RFAstates[brd]->RFVNpp = val;
-         RFAstates[brd]->RFVNpp = Filter * val + (1-Filter) * RFAstates[brd]->RFVNpp;       
+         else RFAstates[brd]->RFVNpp = Filter * val + (1-Filter) * RFAstates[brd]->RFVNpp;
       }
       // Calculate the DC power input
       RFAstates[brd]->DCpower = RFAstates[brd]->DCV * RFAstates[brd]->DCI;
@@ -1462,12 +1510,8 @@ void RFAupdateQUAD(int Module)
   {
     // Calculate the gain compensation factor based on the calibration frequency and the current operating
     // frequency.
-    if(RFAarray[b]->EnableComp)
-    {
-       // Here if compensation is enabled so calculate the correction factor. 
-       gc = 1.0 + (RFAarray[b]->CalFrequency - RFAarray[b]->Freq)/1000000.0 * RFAarray[b]->FreqComp;
-    }
-    else gc = 1.0;
+    gc = RFAgainComp(b);
+
     SelectBoard(b);
     SelectRange(b, RFAarray[b]->SetPoint);
     setRFADAC(b, RFAdacSETPOINT,RFAarray[b]->SetPoint, gc);
@@ -1515,12 +1559,8 @@ void RFAacquire(void)
     {
       // Calculate the gain compensation factor based on the calibration frequency and the current operating
       // frequency.
-      if(RFAarray[b]->EnableComp)
-      {
-        // Here if compensation is enabled so calculate the correction factor. 
-        gc = 1.0 + (RFAarray[b]->CalFrequency - RFAarray[b]->Freq)/1000000.0 * RFAarray[b]->FreqComp;
-      }
-      else gc = 1.0;
+      gc = RFAgainComp(b);
+
       SelectBoard(b);
       SelectRange(b, RFAarray[b]->SetPoint);
       setRFADAC(b, RFAdacSETPOINT,RFAarray[b]->SetPoint, gc);
@@ -1707,6 +1747,7 @@ void RFAgetAutoRange(int Module)
 
   if((b = RFAmodule2board(Module)) == -1) return;
   SendACKonly;
+  if(SerialMute) return;
   if(RFAarray[b]->AutoRangeEna) serial->println("TRUE");
   else serial->println("FALSE");
 }
@@ -1732,6 +1773,7 @@ void RFAgetFreqComp(int Module)
 
   if((b = RFAmodule2board(Module)) == -1) return;
   SendACKonly;
+  if(SerialMute) return;
   if(RFAarray[b]->EnableComp) serial->println("TRUE");
   else serial->println("FALSE");
 }
@@ -1751,6 +1793,7 @@ void RFAgetCalFreq(int Module)
 
   if((b = RFAmodule2board(Module)) == -1) return;
   SendACKonly;
+  if(SerialMute) return;
   serial->println(RFAarray[b]->CalFrequency);
 }
 
@@ -1774,6 +1817,7 @@ void RFAgetCompG(int Module)
 
   if((b = RFAmodule2board(Module)) == -1) return;
   SendACKonly;
+  if(SerialMute) return;
   serial->println(RFAarray[b]->FreqComp,4);
 }
 
@@ -1950,11 +1994,11 @@ void setRFArev3(int module)
   RFAarray[b]->ADCresDCCtrl[0].m = RFAarray[b]->ADCresDCCtrl[1].m = 50;
   RFAarray[b]->ADCresDCCtrl[0].b = RFAarray[b]->ADCresDCCtrl[1].b = 32000;
   // Positive HV readback
-  RFAarray[b]->ADCresDCCtrl[2].m = 100;
-  RFAarray[b]->ADCresDCCtrl[2].b = 0;
+  RFAarray[b]->ADCresDCCtrl[DCmonHVP].m = 100;
+  RFAarray[b]->ADCresDCCtrl[DCmonHVP].b = 0;
   // Negative HV readback
-  RFAarray[b]->ADCresDCCtrl[2].m = -100;
-  RFAarray[b]->ADCresDCCtrl[2].b = 65535;
+  RFAarray[b]->ADCresDCCtrl[DCmonHVN].m = -100;
+  RFAarray[b]->ADCresDCCtrl[DCmonHVN].b = 65535;
   for(int i=0;i<4;i++) RFAarray[b]->ADCresDCCtrl[i].Chan = i;
   SendACK;
 }
@@ -1971,6 +2015,8 @@ void calRFApoleSupplies(int module)
 
   if((b = RFAmodule2board(module)) == -1) return;
   if(RFAarray[b]->Rev != 3) BADARG;
+  // Capture the supply state so it can be restored on exit
+  bool hvWasOn = (digitalRead(PWR_ON) == LOW);
   // Turn on the HV power supply
   digitalWrite(PWR_ON,LOW);
   for(int i=0;i<2;i++)
@@ -2030,5 +2076,10 @@ void calRFApoleSupplies(int module)
   serial->print("HV- ADC m = "); serial->print(RFAarray[b]->ADCresDCCtrl[DCmonHVN].m);
   serial->print(" b = "); serial->println(RFAarray[b]->ADCresDCCtrl[DCmonHVN].b);
 
+  // Restore the supply to its entry state. The procedure turns it off to read the
+  // power-off points and previously left it off.
+  digitalWrite(PWR_ON, hvWasOn ? LOW : HIGH);
+  // Force the loop to resync the poles; the DACs were left at the calibration values.
+  RFAupdate = true;
   SendACK;
 }
\ No newline at end of file
diff --git a/src/Serial.cpp b/src/Serial.cpp
index db93646..7a957e4 100644
--- a/src/Serial.cpp
+++ b/src/Serial.cpp
@@ -29,8 +29,8 @@
 #include "Filament.h"
 #include "WiFi.h"
 #include "ethernet.h"
-#include "arb.h"
-#include "adcdrv.h"
+#include "ARB.h"
+#include "ADCdrv.h"
 #include "TWIext.h"
 #include <ThreadController.h>
 #include "Variants.h"
